import { DeliveryMSystemPipe } from './delivery-msystem.pipe';

describe('DeliveryMSystemPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryMSystemPipe();
    expect(pipe).toBeTruthy();
  });
});
